<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <footer class="bg-blue-900 text-white py-8">
        <div class="container mx-auto px-4">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div>
                    <h3 class="text-xl font-semibold mb-4">About FloodSheild</h3>
                    <p class="text-gray-300">A comprehensive flood monitoring and response system to help communities prepare and respond to flood situations.</p>
                </div>
                <div>
                    <h3 class="text-xl font-semibold mb-4">Quick Links</h3>
                    <ul class="space-y-2">
                        <li><a href="/about" class="text-gray-300 hover:text-white transition duration-300">About Us</a></li>
                        <li><a href="/checklist" class="text-gray-300 hover:text-white transition duration-300">Emergency Checklist</a></li>
                        <li><a href="/educator" class="text-gray-300 hover:text-white transition duration-300">Flood Education</a></li>
                    </ul>
                </div>
                <div>
                    <h3 class="text-xl font-semibold mb-4">Emergency Contacts</h3>
                    <ul class="space-y-2 text-gray-300">
                        <li>NDRF: 1078</li>
                        <li>Emergency: 112</li>
                        <li>Flood Control Room: 1800-180-1551</li>
                    </ul>
                </div>
            </div>
            <div class="border-t border-blue-800 mt-8 pt-8 text-center text-gray-300">
                <p>&copy; 2024 FloodSheild. All rights reserved.</p>
            </div>
        </div>
    </footer>
    
    <style>
        #footer {
            position: fixed;
            bottom: 0;
            left: 0;
            transform: translateY(100%);
            z-index: 40;
        }

        #footer.visible {
            transform: translateY(0);
        }
    </style>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const footer = document.getElementById('footer');
            let lastScrollTop = 0;

            window.addEventListener('scroll', function() {
                const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
                const windowHeight = window.innerHeight;
                const documentHeight = document.documentElement.scrollHeight;

                // Show footer when near bottom of page
                if (documentHeight - (scrollTop + windowHeight) < 100) {
                    footer.classList.add('visible');
                } else {
                    footer.classList.remove('visible');
                }

                lastScrollTop = scrollTop;
            });
        });
    </script>

</body>
</html>